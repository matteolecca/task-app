import React from "react";
import Carousel from "react-elastic-carousel";

const C = props => {

    return (
        <Carousel >
           {props.children}
        </Carousel>
    )

}

export default (C)

